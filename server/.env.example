# SmartSolve AI - Environment Configuration
# Copy this file and rename it to .env for local development

# Required: Your Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Required: Secret key for JWT token generation (use a long random string)
JWT_SECRET_KEY=your_secure_random_secret_key_here

# Optional: Google OAuth Configuration (leave empty to disable Google login)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Frontend URL (for OAuth redirects)
# Development: http://localhost:3000
# Production: Your actual frontend URL (e.g., https://smartsolve-ai.vercel.app)
FRONTEND_URL=http://localhost:3000

# ============================================
# For Render Deployment:
# ============================================
# Add these environment variables in Render dashboard:
# 
# Required:
#   GEMINI_API_KEY = (your Gemini API key)
#   JWT_SECRET_KEY = (generate a secure random string)
#   FRONTEND_URL = https://your-frontend-url.vercel.app
#
# Optional (for Google OAuth):
#   GOOGLE_CLIENT_ID = (from Google Cloud Console)
#   GOOGLE_CLIENT_SECRET = (from Google Cloud Console)
#
# Make sure the Google Cloud Console OAuth redirect URI is set to:
#   https://smartsolve-ai.onrender.com/auth/google/callback
